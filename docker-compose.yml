version: '3.8'

services:
  voice-agent:
    build: .
    container_name: voice-story-agent
    ports:
      - "8082:8082"
      - "8501:8501"
    env_file:
      - .env
    environment:
      - BACKEND_API_URL=http://localhost:8082
    restart: unless-stopped
